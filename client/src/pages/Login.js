import React from "react";

function Login() {
  return (
    // "title" gives the div in the component a target so that the test can query by title and find it the component rendered
    <div title="login">
      <h1>Login Page</h1>
    </div>
  );
}

export default Login;
